#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../acs/bin/acs-debug" "$@"
  ret=$?
else 
  node  "$basedir/../acs/bin/acs-debug" "$@"
  ret=$?
fi
exit $ret
