/**
 * this controller is useful when running a full nettle
 * install on a test machine
 */

exports.name = 'Local (testing only)';

exports.setup = function(m, callback)
{
	callback(true,{'type':'local'});	
};

